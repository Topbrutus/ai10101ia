version: 1
description: >
  Schéma métier de l'entité DIEU — AI10101IA (T0018).
  Définit les champs obligatoires, optionnels et les contraintes de l'entité Dieu.
generated_for: T0018
cree_le: "2026-03-12T00:00:00Z"

schema:
  type: DIEU
  version: 1
  champs_obligatoires:
    - nom: id
      type: string
      format: "DIEU-[0-9]{3}"
      description: Identifiant canonique unique du dieu
    - nom: nom
      type: string
      description: Nom propre du dieu
    - nom: domaine
      type: string
      description: Domaine fonctionnel de responsabilité
    - nom: rang
      type: string
      enum: [primaire, secondaire, tertiaire, quaternaire]
      description: Rang hiérarchique du dieu
    - nom: responsabilites
      type: list[string]
      description: Liste des responsabilités du dieu
    - nom: statut
      type: string
      enum: [actif, inactif, archive]
      description: Statut courant du dieu
  champs_optionnels:
    - nom: hub_id
      type: string
      description: Hub d'appartenance
    - nom: relations
      type: list[relation]
      description: Relations avec d'autres entités
    - nom: metadonnees
      type: dict
      description: Métadonnées libres
  contraintes:
    - La combinaison (domaine, rang) doit être unique dans le registre
    - Au plus 12 dieux de rang primaire sont autorisés
    - Un dieu actif ne peut pas avoir de hub_id nul si des académies lui sont rattachées
